PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/job-tracker
CLIENT_ORIGIN=http://localhost:3000

# Must match the AUTH_SECRET used by the Next.js client — it verifies the
# short-lived API tokens the client mints after a successful Auth.js sign-in.
AUTH_SECRET=<YOUR_AUTH_SECRET>
